$( document ).ready(function() {
    jQuery(function($) {
	$('#buttons a').click( function () {
		var sex = $(this).attr("name");
		$.ajax({
			url: '/api/wow/' + race + '/' + sex,
			success: function(html) {
				$('#nick').html(html);
			}
		});
	});
});

});