<%= simple_form_for @game do |f| %>
  <%= f.input :title %>
  <%= f.input :name %>
  <%= f.input :slug %>
  <%= f.input :content %>
  <%= f.input :description %>

  <%= f.button :submit, value: 'Сохранить' %>
<% end %>
